/**
 * Stripe booking link — update this single constant to enable all booking buttons site-wide.
 * Buttons that use this: Navbar "Book a Call", Pricing page "Book Your Call" / "Book Now",
 * Testimonials page "Book a Consultation".
 */
export const STRIPE_BOOKING_URL = '#';
